{
  "name": "letta",
  "private": true,
  "type": "module",
  "description": "Letta — autonomous invoice-factoring agent on Arc, settled in USDC (Ignyte Stablecoins Commerce Stack, Track 2)",
  "scripts": {
    "test": "node --import tsx --test test/**/*.test.ts",
    "typecheck": "tsc --noEmit",
    "app": "tsx server.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.105.0",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
